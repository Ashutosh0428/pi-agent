# Dependencies for the Streamlit Cloud web demo (streamlit_app.py).
# Local CLI users should `pip install -e .` instead (see pyproject.toml).
streamlit>=1.32
anthropic>=0.40
openai>=1.53
pandas>=2.0
python-pptx>=0.6.21
openpyxl>=3.1
